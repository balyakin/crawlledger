# CrawlLedger protect server v1 site=example
if ($crawlledger_example_fixture_static_deny) {
    return 403;
}
limit_req_status 429;
limit_req zone=crawlledger_example_fixture_emergency burst=2 nodelay;
