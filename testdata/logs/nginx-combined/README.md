# Nginx combined fixtures

`valid.log` has three accepted synthetic records and one empty skipped line. `invalid.log` has three rejected records. `privacy.log` contains unique canaries that must not survive normalization or publication.
