# Caddy JSON fixtures

`valid.jsonl` has two accepted standard Caddy access records. Upstream and cache metrics are intentionally unavailable. `invalid.jsonl` contains invalid timestamp and header types.
