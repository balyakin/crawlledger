# Synthetic fixture policy

Every fixture in this tree is synthetic. Addresses use RFC 5737/3849 documentation ranges, domains use `.example`, and tokens are invented or public crawler product names. Never copy production access logs into this directory.

Expected valid/invalid counts are documented beside each format. Golden files may only be regenerated by an explicit test update mode after a privacy review.
